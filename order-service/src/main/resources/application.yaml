spring:
  application:
    name: order-service
  config:
    import: optional:configserver:http://localhost:8888
  rabbitmq:
#   --for local testing, these values can be overridden by the config server
#    host: localhost
#    port: 5672
#    username: guest
#    password: guest
    host: rabbitmq

queue:
  notification:
    exchange: notification-exchange
    routing-key: notification-routing-key